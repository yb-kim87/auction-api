# 나이스옥션 브라우저 헤더 적용 20건 재검증

## 결과

실제 유효한 물건 ID 20건에 브라우저 요청 형식의 헤더를 적용해 상세 API를
연속 호출했다. 20건 모두 HTTP 200, API `code=0`, `data.obj` 정상 반환이었다.

## 해석

기존 12건 차단은 절대적인 API 한도라기보다 요청 헤더·Referer·브라우저 식별
정보가 부족한 호출에서 발생했을 가능성이 있다. 100건 이상 대량 테스트와
이용약관 검토 전에는 운영 전환을 확정하지 않는다.

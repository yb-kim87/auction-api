# 물건 상세페이지 수익계산기 추가

날짜: 2026-07-14
관련 레포: auction

## 배경

물건 상세페이지에 "입찰가/매도가를 입력하면 최종수익이 얼마인지" 보여주는 수익계산기를
추가해달라는 요청. 사용자가 직접 만들어 쓰던 엑셀 수익률계산시트(경매코치 수익률 계산 시트)의
계산식을 그대로 웹 화면으로 옮기는 방식으로 진행.

## 실행 프롬프트 (요약)

```
물건 상세페이지에 수익계산기를 넣으려고 하는데 내가 기존에 만들어놓은 수익계산 엑셀시트가
있는데 해당 시트내 계산 값들을 보고 수익계산기를 만들어 줄 수 있어?
입찰가(낙찰가) / 매도가를 입력하면 최종수익이 얼마나 되는지에 대한 건데
시트내에 대출계산하는게 있지만 어차피 우리는 대출계산에 대해선 이미 계산식을 만들어뒀으니
그 계산식에 내가 넣은 입찰가/물건 자체 감정가를 적용해서 수치가 기입되게 해주면 될꺼같고
각종 명도비, 미납관리비 같은건 엑셀시트에 적혀있는 값으로 일단 기본 세팅을 해주면 될꺼같아
나머진 수식 참고해서 만들어주면 될꺼같아
수익계산기 버튼 혹은 탭은 레이아웃상 너가 괜찮아 보이게 만들어주면돼
엑셀 파일 경로: E:\OneDrive\auctiondev\수익률계산시트.xlsx (시트: 수익률계산시트)
추가로 양도세율은 (사용자가 보낸) 과세표준 구간별 세율/누진공제표를 참고하여 수식을 넣어줘
(대출계산 연동 방식 확인) 기존 대출계산식 자동 연동
(양도세율 보유기간 반영 여부 확인) 보유기간과 무관하게 항상 구간별 누진세율만 적용
우리는 매매사업자를 활용한 사업자용 세율을 적용할꺼라서 단기매도 해도 저 세율을 적용해도돼
```

## 엑셀 시트 분석

파일이 한글 시트명/셀 인코딩이 깨져 있어(mojibake) openpyxl로 셀 좌표·수식만 추출한 뒀,
사용자가 추가로 보내준 스크린샷("입찰가 산정 및 수익률 분석")으로 각 셀의 실제 라벨을 확인.

핵심 계산 흐름(엑셀 셀 좌표 기준):
- `B4`(입찰가율) = 낙찰가/최저가
- `E7/F7` = 감정가×70%, 낙찰가×80% → `G8`(대출) = `MIN(E7:F7)`
- `G9`(실투자금) = 취득금액합계(B15) − 대출(G8)
- `G10`(수익률%) = 최종수익(B22) / 실투자금(G9)
- `B8`(등기비용) = 낙찰가 × 취득세율(`C8`, 구간별 1~3% + 지방교육세 등 +0.7%)
- `B12`(중개수수료 매도) = 매도가 × 요율(`C12`, 매도가 구간별 0.4~0.7%)
- `B13`(대출이자) = 대출금 × 연이자율(4.5%) / 12 × 보유월수(6)
- `B15`(취득금액합계) = SUM(낙찰가+등기비용+인테리어+명도비+미납관리비+중개수수료+대출이자+중도상환수수료)
- `B18`(매매차익) = 매도가 − 취득금액합계
- `B19`(소득세) = 매매차익 × 양도세율 − 누진공제(엑셀 예시에서는 24% 단일세율만 곱해 실제
  구간별 누진공제표는 비어 있었음 — 이번에 사용자가 준 표로 정식 반영)
- `B22`(최종수익) = 매매차익 − 소득세 − 부동산추가수수료 − 부가세

엑셀 값(최저가 245,000,000 / 감정가 350,000,000 / 낙찰가 255,000,000 / 매도가 300,000,000
/ 보유6개월)으로 대출·등기비·이자·중개수수료·취득금액합계·실투자금·매매차익까지 전부 검증해
동일한 값이 나오는 것을 확인함(대출 204,000,000 / 등기비 4,590,000 / 대출이자 4,590,000 /
중개수수료 1,200,000 / 취득금액합계 268,380,000 / 실투자금 64,380,000 / 매매차익 31,620,000).

## 양도세율(양도소득세) 처리

사용자가 제공한 과세표준 구간별 세율/누진공제표:

| 과세표준 | 세율 | 누진공제액 |
|---|---|---|
| 14,000,000 이하 | 6% | 0 |
| 14,000,000 초과 ~ 50,000,000 이하 | 15% | 1,260,000 |
| 50,000,000 초과 ~ 88,000,000 이하 | 24% | 5,220,000 |
| 88,000,000 초과 ~ 150,000,000 이하 | 35% | 14,900,000 |
| 150,000,000 초과 ~ 300,000,000 이하 | 38% | 19,400,000 |
| 300,000,000 초과 ~ 500,000,000 이하 | 40% | 25,400,000 |
| 500,000,000 초과 ~ 1,000,000,000 이하 | 42% | 35,400,000 |
| 1,000,000,000 초과 | 45% | 65,400,000 |

**보유기간과 무관하게 항상 이 구간별 누진세율표만 적용한다.** 매매사업자를 활용한
사업자용 세율 구조라 개인 단기양도(1년 미만 70% 등) 세율 체계는 도입하지 않음.

## 변경 내용 (auction)

- `src/lib/profit-calculator.ts` 신규
  - `acquisitionTaxRate(minPriceWon)`: 취득세율(구간별 1~3% + 1.1배 + 0.7%p)
  - `saleBrokerageRate(salePriceWon)`: 매도 중개수수료율(구간별 0.4~0.7%)
  - `capitalGainsTaxBracket(taxBaseWon)`: 양도세 구간별 세율/누진공제(위 표)
  - `calculateProfit(input)`: 대출한도부터 최종수익·수익률까지 전체 계산
  - `isOver85Sqm(area)`: 물건의 `area` 문자열에서 전용면적을 파싱해 85㎡ 초과 여부 판정
- `src/components/ProfitCalculatorPanel.tsx` 신규
  - 낙찰가(입찰가) 초기값 = 물건 `minPrice`, 매도가 초기값 = 물건 `appraisedValue`
  - 대출비율(감정가/낙찰가 기준)은 `AuctionDetailModal`이 이미 갖고 있는
    `loanRatio`/`appraisalRatio`(기존 대출정책 계산 결과)로 기본 세팅, 관리자가 필요 시 직접 수정 가능
  - 인테리어(200만원)/명도비(200만원)/미납관리비(100만원)/중도상환수수료(0%)를 기본값으로 세팅
    (2026-07-15 사용자 제공 참고 이미지 기준으로 조정, 아래 追記 참고)
  - 등기비용, 매도 중개수수료는 자동계산 값이라 읽기 전용으로 표시(직접 수정 불가)
  - 85㎡ 초과 물건일 때만 부가세 입력란 노출
- `src/components/AuctionDetailModal.tsx`: "기본정보" / "AI에게 물어보기" 옆에 "수익계산기" 탭
  추가(회원 상세보기 모드에서만 노출, 편집 모드에서는 숨김)

## 결과
- 상세페이지에서 낙찰가·매도가·보유기간 등을 바꾸면 실시간으로 대출금·실투자금·수익률·최종수익이
  재계산됨
- 대출한도 계산은 기존 대출정책 로직과 동일한 값으로 시작하되, 필요시 시나리오별로 직접
  비율을 조정해볼 수 있음

## 추가: 기본값/UI 조정 (2026-07-15 追記)

### 실행 프롬프트 (요약)
```
1. 이미지1을 참고하여 이미지값들을 기본값으로 넣어줘 (인테리어 200만/명도비 200만/미납관리비 100만)
2. 보유기간 기본값은 4개월로 해줘
3. 상단에 입찰가율 정보는 필요 없을꺼같은데 이유가 있으면 말해줘
4. 제일 하단에 계산 상세에서 누진공제 적용여부를 체크할 수 있도록 해줘
그리고 추가로: 낙찰가 기본값은 최저가로, 매도가는 감정가로 넣어줘
(낙찰가=minPrice, 매도가=appraisedValue는 최초 구현 때부터 이미 그렇게 되어 있어 추가 변경 불필요)
```

### 변경 내용
- `src/components/ProfitCalculatorPanel.tsx`
  - 보유기간 기본값 6개월 → 4개월
  - 인테리어 300만원 → 200만원, 명도비 0원 → 200만원, 미납관리비 0원 → 100만원
  - 상단 요약 카드에서 "입찰가율"(낙찰가/최저가, 계산 결과에 영향 없는 참고 지표) 제거
  - "계산 상세" 섹션에 "누진공제 적용" 체크박스 추가(기본 체크됨). 해제하면 양도세를
    매매차익×세율로만 계산하고 누진공제를 빼지 않음
- `src/lib/profit-calculator.ts`
  - `ProfitCalculatorInput`에 `applyProgressiveDeduction: boolean` 추가
  - `calculateProfit`이 이 플래그에 따라 `capitalGainsTaxDeduction`을 0으로 처리할지 결정

## 추가: 부가세 기본값 자동 계산 (2026-07-15 追記)

### 배경
부가세 입력란은 85㎡ 초과 물건에서만 나오도록 이미 구현되어 있었음(정상 동작, 사용자가
85㎡ 이하 물건에서 확인해 "빠졌다"고 오인한 것으로 확인). 다만 초기값이 0이라 매번
직접 계산해 입력해야 하는 불편이 있어, 엑셀 시트의 관행대로 자동 기본값을 넣어달라는 요청.

### 실행 프롬프트 (요약)
```
좋아 그럼 부가세 값에 대한 기본값을 매도가 10% * 50% 곱한 값을 써줘
만약 매도가가 9억이라면 4500만원인거지
```

### 변경 내용
- `src/components/ProfitCalculatorPanel.tsx`
  - 부가세(`vatAmount`) 초기값 = `매도가 × 0.1 × 0.5`(85㎡ 초과일 때만)
  - 매도가를 바꾸면 부가세도 같은 공식으로 자동 재계산(`useEffect`)되지만, 사용자가
    부가세 입력란을 한 번이라도 직접 수정하면(`vatEdited`) 그 이후 자동 갱신은 멈추고
    입력값을 그대로 유지

### 결과
- 85㎡ 초과 물건에서 매도가 9억 → 부가세 4,500만원이 자동으로 채워짐
- 필요하면 관리자가 부가세 값을 직접 덮어쓸 수 있고, 그 뒤로는 매도가를 바꿔도
  자동으로 되돌아가지 않음

## 추가: 추천물건 카드에 "추정 수익" 표시 (2026-07-15 追記)

### 배경
홈 화면 카드의 "최소 투자금" 박스 옆에 수익계산기의 기본값(낙찰가=최저가, 매도가=감정가,
계산기 기본 비용값)으로 계산한 최종수익을 "추정 수익"으로 함께 보여달라는 요청.

### 실행 프롬프트 (요약)
```
그리고 이제 우측에 추정 수익 이라고 값을 넣어줄건데 이 값은 이미지2에서 보이는 우리가 방금
만든 수익계산기에 기본값으로 이뤄진 최종수익을 넣어주면 될꺼같아
```

### 변경 내용
- `src/lib/profit-calculator.ts`
  - `estimateDefaultProfit(params)` 신규: `ProfitCalculatorPanel`의 초기 입력값을 그대로
    재현(낙찰가=최저가, 매도가=감정가, 보유4개월, 인테리어200만·명도비200만·미납관리비100만,
    이자율4.5%, 부가세=85㎡초과 시 매도가×10%×50%, 누진공제 적용)해 `calculateProfit`을 호출
  - 대출비율(감정가/낙찰가 기준)은 물건에 적용된 `loanInfo.appraisalRatio`/`loanInfo.loanRatio`를
    그대로 사용
- `src/app/page.tsx`
  - `RecommendCard`(그리드뷰 카드)에서 `estimateDefaultProfit`으로 `estimatedProfit` 계산
  - 기존 "최소 투자금" 단일 박스를 좌(최소 투자금)/우(추정 수익) 2단 레이아웃으로 변경,
    구분선(`border-l`)으로 분리
  - 리스트뷰(`RecommendListRow`)는 이번 범위에서 제외(카드형만 적용)

### 결과
- 추천물건 그리드 카드에서 물건별 예상 최종수익을 별도 계산기를 열지 않고도 바로 확인 가능
- 계산기에서 값을 바꿔가며 시뮬레이션한 결과와, 카드에 보이는 기본값 기준 추정치가
  같은 계산 로직(`calculateProfit`)을 공유해 서로 어긋나지 않음

## 추가: 목표수익 이상 물건만 표시하는 필터 (2026-07-15 追記)

### 배경
카드에 "추정 수익"이 노출되니, 회원이 투자정보에 입력해둔 "목표수익"(`profile.targetReturn`,
금액 문자열)보다 추정 수익이 낮은 물건은 애초에 추천 목록에서 안 보이게 해달라는 요청.

### 실행 프롬프트 (요약)
```
좋아 그럼 이제 추정수익이 나오니까 투자정보에서 목표수익을 입력받으니까
목표수익 이상인 물건들만 보여주게 만들어줘
```

### 확인한 사실
- 투자정보의 "목표 수익"(`targetReturn`)은 퍼센트가 아니라 **금액**이다
  (`data/investment-options.ts`의 `TARGET_AMOUNT_WON_STEPS`, "500만원"~"1억 이상" 등).
  추정 수익도 원 단위 금액이라 단위가 그대로 일치해 바로 비교 가능.

### 변경 내용 (auction)
- `src/app/page.tsx`
  - `matchesRecommendFilters`에 `loanInfo`, `targetReturnWon` 파라미터 추가 — 목표수익이
    설정돼 있으면 `estimateDefaultProfit`으로 계산한 추정 수익이 목표수익 미만인 물건은
    `filteredItems`에서 제외
  - `favoritesOnly`와 동일하게 **서버 페이지네이션 이후 클라이언트에서 한 번 더 거르는
    방식**이다. 서버가 내려준 한 페이지(PAGE_SIZE) 중 일부가 필터에 걸려 빠지면, 화면에
    보이는 개수가 그만큼 줄어들 수 있다(서버 필터가 아니므로 "더 보기"로 채워지지 않음).
    현재는 이 한계를 감수하고 클라이언트 필터로 구현함 — 추후 체감 문제가 있으면 서버
    필터(API 파라미터)로 옮기는 것을 검토.

### 결과
- 목표수익을 설정한 회원에게는 그 이상 수익이 예상되는 물건만 추천 목록에 노출됨

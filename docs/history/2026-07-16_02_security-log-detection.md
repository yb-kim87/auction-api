# 로그 감지 시스템 — 이상행위(대량요청/크롤링/자동화) 알림

날짜: 2026-07-16
관련 레포: auction, auction-api

## 요청 원문

```
2. 로그 감지시스템 - 알림
2-1) 대량요청, 크롤링 혹은 자동화 스크립트가 의심되는 경우에 알림이 오는 기능을 구현할 것.
=> 로그 파일을 만들어서 AI가 읽어서 판단하게할 것
```

## 설계 결정 (사전 확인)

1. **로깅 범위**: 인증 포함 모든 API 요청(IP, 경로, 시각, 사용자, 상태코드, 처리시간,
   User-Agent). 특정 엔드포인트만 보는 대신 전체를 남겨야 크롤링처럼 여러 경로를
   순차적으로 훑는 패턴을 잡을 수 있음
2. **판단 주기/알림 채널**: 10분 간격 배치 분석 + 텔레그램 알림(기존
   `kakao-notify/telegram-alert.service.ts` 재사용). 관리자가 수동으로 누르는
   방식이 아니라 자동 스케줄러로 상시 감시

## 구조

```
모든 요청 → RequestLogMiddleware → logs/requests.log (JSON Lines)
                                          ↓ (10분마다)
                          SecurityLogAnalyzerService
                          - 최근 10분 로그만 필터
                          - IP별 통계로 압축(요청수/최소간격/경로수/로그인여부/오류율/UA)
                          - OpenAI에 판단 요청(suspicious 여부 + 요약 + 의심 IP)
                                          ↓ suspicious=true면
                          TelegramAlertService → 관리자 텔레그램 알림
```

## 변경 내용 (auction-api)

- `src/security-log/request-log-writer.service.ts`: 로그 파일 append(JSON Lines),
  20MB 초과 시 회전(rotate), 전체 읽기. 기록 실패해도 요청 처리는 막지 않음
- `src/security-log/request-log.middleware.ts`: 전역 미들웨어. `res.on("finish")`
  시점에 비동기로 로그 기록(응답 지연 없음). `AUTH_TOKEN_COOKIE`에서 access 토큰을
  검증해 로그인 사용자명도 함께 기록
- `src/security-log/security-log-analyzer.service.ts`:
  - 10분마다 로그 파일을 파싱해 최근 10분 구간만 필터
  - IP별로 묶어 요청수·최소 요청간격(자동화 스크립트는 간격이 일정하고 짧은 경향)·
    접근 경로 수(크롤링은 여러 경로를 순회)·로그인 여부·4xx 오류율·User-Agent를 집계,
    상위 20개 IP만 프롬프트에 포함(토큰 절약)
  - `OpenAiService.answerFreeform()`으로 시스템 프롬프트에 판단 기준(대량요청/짧고
    일정한 간격/비로그인 다중경로 순회/높은 오류율/의심스러운 User-Agent)을 제시하고
    JSON으로 `{suspicious, summary, suspiciousIps}` 응답받음
  - suspicious=true면 텔레그램 발송
  - OPENAI_API_KEY 또는 TELEGRAM 설정이 없으면 스케줄러 자체가 비활성화(조용히 스킵)
- `src/security-log/security-log.controller.ts`: 관리자 전용
  `POST /security-log/analyze-now`(즉시 분석), `GET /security-log/recent`(최근 로그
  200줄 조회)
- `src/app.module.ts`: `NestModule` 구현, `RequestLogMiddleware`를 모든 라우트(`*`)에
  전역 적용
- `AiModule`/`KakaoNotifyModule`에서 `OpenAiService`/`TelegramAlertService`를
  export하도록 추가(다른 모듈에서 재사용 가능하게)

## 변경 내용 (auction)

- `src/lib/api.ts`: `analyzeSecurityLogNow()`, `fetchRecentSecurityLog()` 추가
- `src/app/admin/SecurityLogTab.tsx` 신규: 최근 요청 로그 테이블(IP/경로/사용자/
  상태코드/처리시간), "지금 바로 분석 실행" 버튼
- 관리자 콘솔에 "보안 로그" 탭 추가

## 오탐 예외 처리 (2026-07-16 추가)

배포 후 실제로 10분 간격으로 반복해서 "이상 요청 패턴" 텔레그램 알림이 옴.
의심 IP가 `34.116.x`, `35.187.x`, `35.243.x`(GCP 대역)였고, 알림 주기가
`SecurityLogAnalyzerService`의 10분 분석 주기와 정확히 일치 — 사용자 확인
결과 인스타그램 인스턴트(구글시트) 연동에 쓰는 구글 서비스 계정
(`GOOGLE_SERVICE_ACCOUNT_EMAIL`) 관련 정상 트래픽으로 판단됨.

- `src/security-log/security-log-analyzer.service.ts`에 `EXCLUDED_IPS`
  추가, `runOnce()`의 최근 로그 필터링 단계에서 해당 IP는 통계/AI 판단
  대상에서 아예 제외. **대역 전체가 아니라 실제 알림에 등장한 정확한 IP
  6개만** 화이트리스트함(`34.116.22.6`, `35.187.134.140`, `35.243.23.37`,
  `35.187.134.141`, `34.116.21.33`, `35.243.23.39`) — 대역 전체를 막으면
  같은 GCP 대역에서 오는 다른 실제 위협까지 놓칠 수 있어 범위를 넓히지 않음
- 구글 서비스 계정이 다른 IP로 요청하게 되면(주소가 바뀌면) 같은 알림이
  다시 발생할 수 있음 — 그때는 새로 등장한 IP를 목록에 추가할 것

## User-Agent 기반 예외 추가 (2026-07-17/18)

- Google Apps Script(인스타그램 인스턴트 구글시트 연동)는 GCP 임대 IP를 매번 다르게
  써서 IP 화이트리스트를 계속 추가해야 하는 문제. User-Agent에 `Google-Apps-Script`가
  포함되면 예외 처리(`EXCLUDED_USER_AGENT_SUBSTRINGS`).
- 개발자 본인이 로컬/서버에서 node 스크립트(curl 테스트 등)로 관리자 계정 로그인 후
  테스트할 때도 오탐 다수 발생. User-Agent가 정확히 `"node"`이고 **동시에** 로그인
  사용자명이 `admin`일 때만 예외 처리(`EXCLUDED_USER_AGENT_EXACT` +
  `EXCLUDED_UA_REQUIRES_ADMIN_USERNAMES`) — User-Agent만으로 전역 예외하면 공격자가
  UA를 흉내 낼 수 있는 구멍이 생기므로, 로그인 조건을 반드시 함께 건다.

## admin 계정 자체를 통계에서 제외 (2026-07-22)

배포 후에도 관리자 본인이 화면을 정상 사용할 때(`/users/me`, `/recommendations`,
`/recommendations/strategy-labels`, `/favorites` 등을 페이지 로드마다 연속 호출)
"이상행위 감지" 텔레그램 알림이 계속 옴 — 사용자가 로그 스크린샷으로 신고. User-Agent가
브라우저(`Mozilla/...`)라 기존 node UA 예외 조건에 걸리지 않았고, IP도 매번 달라져 IP
화이트리스트로는 못 막는 상황이었음.

- `isExcludedUsername()` 추가, `username === "admin"`인 요청은 통계 집계(`runOnce`의
  `recent` 필터링) 단계에서 제외. **로그 파일에는 그대로 기록되므로 감사 추적은
  유지**되고, 이상행위 "판단" 대상에서만 빠진다 — 계정 탈취 후 오남용까지 완전히
  놓치지 않기 위한 절충.
- 검토했으나 채택하지 않은 대안: 경로(path) 자체를 화이트리스트하는 방식. 범위가
  너무 넓어져(`/auctions`, `/favorites`, `/recommendations` 등 대부분의 API가 대상이
  됨) 로그인 없이 그 경로들을 두드리는 진짜 공격까지 놓칠 위험이 있어 배제. 사용자
  결정: "admin 예외만 유지, 경로는 추가 안 함".

## 텔레그램 알림에 계정/경로 정보 추가 (2026-07-22)

기존 알림 메시지에는 의심 IP만 나열되어 어떤 계정으로 어떤 경로를 두드렸는지 알림만
보고는 알 수 없었음(사용자 피드백: "username이랑 path도 같이 보여주면 좋을 것 같아").

- `runOnce()`에서 AI가 지목한 `suspiciousIps`(또는 없으면 전체 통계)를 이미 집계해둔
  `IpStat`(usernames, paths)과 매칭해, 알림 메시지에 IP별로
  `- {ip} (계정: {usernames} / 경로: {paths 최대 5개} 외)` 형태로 함께 표시.
  AI에게 매번 정확한 포맷으로 계정/경로까지 뽑아달라고 하는 대신, 이미 계산해둔 통계를
  재사용하는 방식을 택함(AI 응답 포맷 불안정성 회피).

## 로그 저장을 파일에서 DB 테이블로 전환 (2026-07-22)

파일(`logs/requests.log`) 방식은 위 "참고사항"에서 이미 지적했던 대로 Railway
재배포 시 컨테이너 파일시스템이 초기화돼 로그가 사라지고, 20MB 넘으면 회전만
될 뿐 옛 로그를 어디에도 보관하지 않는 문제가 있었음. 로그가 쌓이면 느려질까
걱정되면서도, 나중에 문제 조사를 위해 로그를 남겨두고 싶다는 요청(사용자,
2026-07-22)에 따라 구글시트/로컬 파일 등 대안을 검토한 뒤 **DB 테이블**로
결정 — 이미 Postgres가 붙어있어 추가 인프라 없이 가능하고, SQL 조회가
grep보다 빠르며, 재배포에도 안전.

- `src/security-log/request-log.entity.ts` 신규: `request_logs` 테이블(`ts`에
  인덱스). 마이그레이션 `1784234000000-AddRequestLogsTable.ts`.
- `RequestLogWriterService`: 파일 append/readAll → Repository
  insert/`findSince(since)`/`findRecent(limit)`로 전환.
- `SecurityLogAnalyzerService.runOnce()`: 파일 파싱(`parseLogLines`) 제거,
  `findSince(cutoffDate)`로 최근 구간만 DB에서 직접 조회.
- **보관 정책**: `purgeOld()` 추가, 30일 지난 로그를 1시간 간격으로 자동
  삭제(`PURGE_INTERVAL_MINUTES`) — 이 스케줄러는 OpenAI/텔레그램 키 설정
  여부와 무관하게 항상 동작해야 하므로 AI 분석 스케줄러와 분리해서 등록.
- 로그 양이 너무 많아지면(30일 보관으로도 부족하면) 그때 DB에서 로컬로 내려
  받아 보관하는 방식을 쓰기로 함(사용자 결정) — 지금은 자동 아카이빙 별도
  구현 없이 30일 자동 삭제만 적용.
- `.gitignore`에 `logs/` 추가(파일 로그 잔재 정리).

## node UA 예외를 admin 전용에서 로그인 계정 전체로 확대 (2026-08-19)

사용자 신고: "최근 우리가 텔레그램으로 전송하는 보안 로그들을 보면 회원들의
간단한 행동들도 전부다 보고가 되고 있는거 같긴한데 한번 보고 개선할만한
사항이 있는지 알려줄래?"

운영 DB의 최근 `request_logs`를 직접 조회해 확인한 결과, 프론트(Vercel)가
브라우저 요청을 서버사이드로 프록시해 백엔드로 넘기는 구조 때문에 관리자든
일반 회원이든 정상적으로 사이트를 쓰기만 해도 원래 브라우저 UA 대신 Node의
기본 User-Agent(`"node"`)로 찍히고 있었다. 그런데 `isExcludedUserAgent()`의
"node UA + 로그인된 요청" 예외 조건이 `EXCLUDED_UA_REQUIRES_ADMIN_USERNAMES
= ["admin"]`로 admin 계정에만 한정돼 있었다(2026-07-18, 당시엔 개발자 본인
테스트 요청 오탐만 막으면 된다고 판단해 좁게 잡았음) — 그 결과 일반 회원
(예: `park`)의 정상적인 사이트 이용까지 계속 "정체불명 UA로 여러 경로 접근"
패턴에 걸려 AI 판단기로 넘어가 오탐 알림이 반복되고 있었다.

**수정**: `src/security-log/security-log-analyzer.service.ts`에서
`EXCLUDED_UA_REQUIRES_ADMIN_USERNAMES` 화이트리스트를 없애고,
`isExcludedUserAgent()`가 "node UA + username이 비어있지 않음(=로그인된
요청)"이면 계정 종류와 무관하게 예외 처리하도록 변경. 로그인 없이 접근하는
요청(진짜 스크립트 공격/세션 탈취 전)은 여전히 그대로 분석 대상에 남아
안전성은 유지된다. `ANALYSIS_EXCLUDED_USERNAMES`(admin 전용, UA와 무관하게
통계 집계 자체에서 제외하는 별도 로직, 2026-07-22 추가분)는 이번 변경과
무관하게 그대로 유지.

### 결과
- 타입체크·빌드 통과.
- Railway 배포 후 상태 확인.
- 실제 텔레그램 알림 빈도 감소 여부는 다음 분석 주기(30분 간격) 이후
  자연스럽게 확인될 것 — 별도 인위적 트래픽 생성 테스트는 진행하지 않음.

## 참고사항 / 향후 고려

- Railway 컨테이너는 재배포 시 파일시스템이 초기화될 수 있어, `logs/requests.log`도
  배포 때마다 비워진다. 장기 보관이 필요해지면 DB 테이블이나 외부 로그 서비스로
  옮기는 것을 검토할 것(지금은 요청대로 "로그 파일" 방식을 우선 구현)
- 판단은 규칙 기반이 아니라 매번 AI(OpenAI)에게 맡기므로, 관리자가 정상 트래픽 패턴을
  보고 시스템 프롬프트의 판단 기준을 조정하고 싶을 수 있음 — 현재는 코드에 하드코딩,
  필요시 관리자 화면에서 프롬프트를 수정 가능하게 확장할 수 있음

## 追記 (2026-08-20) — 4일치 실측 리뷰, 로컬호스트(크롤러 자기호출) 오탐 제외 추가

사용자 질문: "적은 시도는 짧은 간격으로 해도 크게 문제 안 되는데 텔레그램 보고를
꼭 해야 하나?"에 답하기 위해, 운영 Postgres `request_logs`를 최근 4일(8/16~8/20)
치로 직접 집계해(30분 윈도우 + 기존 서비스와 동일한 제외 규칙 재현) 실제로 AI가
의심했을 법한 패턴을 리뷰했다(`railway run --service Postgres`로 `DATABASE_PUBLIC_URL`
주입 후 임시 스크립트로 읽기 전용 조회, 조회 후 스크립트는 삭제).

**결과**: 총 3,176건 중 의심 후보 7건 중 4건이 전부 같은 원인의 오탐이었다 —
`::ffff:127.0.0.1`(서버 자기 자신)에서 `/crawler/import-item`으로 매일 밤
23:00(UTC)경 500~750건을 0~1ms 간격으로 호출하는 패턴. 이건 외부 요청이 아니라
`.env`에 문서화된 "크롤 완료 시 로컬 DB + Railway 운영 DB 동시 적재" 배치가 자기
자신에게 보내는 내부 호출인데, 로그인 없음·단일 경로·초고속 반복이라 AI 판단
기준(짧은 시간 대량 요청 + 비정상적으로 일정한 간격)에 정확히 걸려 매일 밤
오탐으로 텔레그램 알림이 갔을 가능성이 높다.

나머지 3건은 AWS 서울 리전 IP(`54.180.x`, `3.38.x`)에서 `/auth/login`을 5~9회
반복 실패시키거나 `/auth/signup`·`/auctions/pending` 등 여러 엔드포인트를 순회한
흔적으로, 요청 수는 적지만 진짜 로그인 시도/스캐닝으로 보이는 패턴이었다 —
"적은 시도라 안전하다"가 항상 성립하지 않는다는 근거로 판단, 이쪽은 알림을 그대로
유지하기로 함(사용자 확인).

**수정**: `SEED_EXCLUDED_IPS`에 로컬호스트 3종(`::ffff:127.0.0.1`, `127.0.0.1`,
`::1`)을 추가. 앱이 최초 기동 시(`onModuleInit` → `seedInitialExclusions`) DB에
없는 것만 채워 넣으므로, 다음 배포부터 자동으로 반영된다. 외부 소규모 로그인
시도류(위 3건)는 기준을 건드리지 않고 그대로 둠.

## 追記 (2026-08-21) — 정상 로그인 1건이 "0ms 간격"으로 오인되던 오탐 수정

사용자가 11:05(KST)에 온 텔레그램 보안 알림을 보고 "우리 내부 문제인지, 이번
강의실 Vercel 프로젝트 분리 작업 때문인지" 확인을 요청함. `railway logs --since/--until`로
해당 시각 로그를 특정하고, `request_logs` 테이블을 대조해 조사한 결과:

- 알림 내용: IP `3.38.255.246`에서 "요청 1건 / node UA / 로그인 없음 / 0ms 간격" →
  AI가 "무차별 대입/스캐닝 의심"으로 판단.
- 실제로는 같은 IP에서 `POST /auth/login`(성공) 직후 `hyewon319` 계정으로
  `/users/me`, `/settings`, `/favorites`가 정상 호출된, **완전히 정상적인 회원
  로그인 흐름**이었다.
- 로그인 성공 이후 요청들은 이미 있던 "로그인된 node UA는 제외" 규칙에 걸려
  분석 대상에서 빠졌지만, **`/auth/login` 요청 자체는 로그인이 성사되기 전이라
  구조적으로 `username`이 항상 빈 값**이라 이 제외 규칙을 못 타고 그대로
  분석 대상에 남는다.
- 여기에 `buildIpStats()`의 `minIntervalMs: Number.isFinite(minIntervalMs) ? minIntervalMs : 0`
  로직이 겹쳐서, 요청이 1건뿐이라 "간격" 자체가 없는 상황을 "0ms"로 잘못
  표기하고 있었다 — AI 입장에선 "요청이 0ms 간격으로 몰아쳤다"는 자동화 스크립트
  신호로 오해하기 딱 좋은 문구였다.
- 즉 **모든 회원의 정상 로그인 시도 1건은 항상 "1건/0ms/로그인 없음"으로 AI에게
  보고되는 구조적 노이즈**였고, AI가 가끔 이걸 의심스럽다고 판단해 알림이 갔던
  것 — 이번 강의실 Vercel 프로젝트 분리(rewrite/basePath 등)와는 무관한, 그
  이전부터 있던 로직 결함으로 확인.

**수정**: `IpStat.minIntervalMs` 타입을 `number | null`로 바꿔 요청 1건짜리
IP는 `null`(간격 정보 없음)로 명확히 구분하고, `summarizeForPrompt()`에서도
"해당없음(요청 1건)"으로 표기하도록 고쳤다. 시스템 프롬프트에도 "요청 1건 +
로그인유저 없음"은 대부분 정상 로그인 시도 자체이니 그것만으로 suspicious=true로
판단하지 말라는 안내를 추가했다(단, 같은 IP에서 로그인 시도가 여러 번 짧은
간격으로 반복되는 진짜 무차별 대입 패턴은 count≥2라 실제 간격이 찍히므로 계속
정상 탐지됨). Railway 배포 후 `Nest application successfully started` +
보안 로그 분석 스케줄러 정상 기동 확인 완료.

## 追記 (2026-09-02) — 현재 구조 및 운영 알림 품질 재점검

사용자 요청에 따라 현재 코드, 최근 7일 운영 `request_logs`, Railway 애플리케이션
로그를 읽기 전용으로 다시 점검했다. 코드는 변경하지 않고 현재 문제와 개선 우선순위만
분석했다.

### 현재 동작

- 모든 API 완료 요청을 `request_logs`에 저장하고 30일 보관한다.
- 30분마다 최근 30분 요청에서 IP·UA·계정 예외를 제외한 뒤 IP별 통계 상위 20개를
  OpenAI에 전달한다.
- AI가 `suspicious=true`를 반환하면 IP별 계정과 접근 경로를 붙여 텔레그램으로 보낸다.
- 분석 결과와 발송 성공 여부는 별도 테이블에 저장하지 않고 애플리케이션 로그에만 남긴다.
- 관리자 화면 안내에는 아직 `10분마다`라고 표시되지만 실제 서버 상수는 30분이다.

### 최근 7일 운영 실측

- 전체 요청 16,750건, 고유 IP 424개, 오류 응답 110건, 인증된 요청 3,182건.
- UA별 요청은 Google Apps Script 9,964건, 브라우저 3,372건, `node` 3,340건,
  기타 74건이었다.
- Railway 로그에서 `이상행위 감지`가 43회 기록됐다.
- 같은 기간 AI JSON 파싱 실패와 텔레그램 HTTP/예외 실패 로그는 확인되지 않았다.
- 43건 중 다수는 비로그인 `node` UA가 1~3건을 수백 ms 간격으로 요청한 패턴을
  자동화로 판단한 사례였다. 시스템 프롬프트가 단발성 요청을 의심하지 말라고 명시해도
  AI가 반대로 판정한 사례가 실제로 남아 있어 오탐 억제를 AI 지시문에만 맡길 수 없다.
- 동일 IP 또는 유사 패턴이 인접한 30분 구간마다 반복 통보되는 사례가 있어 알림
  쿨다운·중복 억제가 필요하다.

### 확인된 구조적 위험

1. **결정적 후보 필터 없음**: 분석 대상 요청이 한 건만 있어도 매 주기 OpenAI를 호출한다.
   비용이 계속 발생하고 작은 정상 요청도 AI 오탐 대상이 된다.
2. **최종 판정을 AI가 전담**: JSON 파싱 실패 시 조용히 정상으로 처리되어 실제 공격을
   놓칠 수 있고, 반대로 프롬프트와 다른 판단으로 오탐을 만들 수 있다.
3. **중복 알림 억제 없음**: IP·패턴별 마지막 알림 시각을 저장하지 않아 같은 현상을
   반복 전송할 수 있다.
4. **인증된 `node` UA 전체 제외**: Vercel 프록시 오탐을 막지만, 탈취된 일반 회원
   세션으로 자동화 요청을 보내면 분석 대상에서 완전히 제외된다. `admin` 계정은 UA와
   무관하게 분석에서 제외돼 계정 탈취 탐지에는 더 큰 사각지대가 있다.
5. **클라이언트 IP 신뢰 경계 불명확**: `x-forwarded-for` 첫 값을 그대로 사용한다.
   신뢰 프록시가 헤더를 항상 덮어쓴다는 보장이 없다면 공격자가 IP를 위조하거나
   화이트리스트 IP를 흉내 낼 가능성이 있다.
6. **분산 공격 탐지 약함**: IP별 요청수 상위 20개만 보므로 여러 IP에 요청을 분산한
   로그인 공격·스캐닝은 놓칠 수 있다.
7. **User-Agent 프롬프트 주입 가능성**: 외부 입력인 UA 문자열을 AI 프롬프트에 그대로
   포함한다. 현재 80자로 자르지만 명령형 문자열에 대한 명시적 이스케이프·불신 처리가 없다.
8. **전송 내구성 부족**: 텔레그램 호출에 명시적 timeout·retry·전송 결과 저장이 없다.
   `send()`가 실패를 내부에서 삼키므로 호출자는 전송 성공 여부를 알 수 없다.
9. **관리 화면 설명 불일치**: 실제 30분 주기인데 10분으로 안내되어 운영 판단을 흐린다.
10. **테스트 부재**: 감지 임계값, 예외 처리, 중복 억제, 텔레그램 실패를 검증하는 자동
    테스트가 확인되지 않았다.

### 권장 개선 순서

1. AI 앞에 코드 기반 후보 필터를 두어 명확한 후보가 없으면 AI 호출 자체를 생략한다.
2. 로그인 실패, 401/403/404 스캐닝, 고속 다중경로, 비정상 대량 요청을 코드 규칙으로
   점수화하고 임계값 이상만 알린다. 1~3건의 단순 `node` 요청은 알리지 않는다.
3. AI는 경계 사례의 설명·요약에만 선택적으로 사용하고 알림 여부를 단독 결정하지 못하게 한다.
4. IP·규칙 코드별 쿨다운과 분석/알림 이력 테이블을 추가해 중복 전송과 실패를 추적한다.
5. 인증 여부만으로 전체 제외하지 말고 정상 프록시임을 확인할 수 있는 내부 서명 헤더 또는
   신뢰 가능한 원본 클라이언트 정보 전달 구조를 도입한다. 관리자 계정도 고위험 행동은 감시한다.
6. 텔레그램에 timeout, 제한적 retry, 성공/실패 반환값을 추가한다.
7. 관리자 화면의 주기 안내를 30분으로 수정하고 최근 판정·발송 상태를 표시한다.

## 追記 (2026-09-02) — 개선 구현 요청 및 범위

사용자가 위 점검 결과의 개선사항 적용을 요청했다. 이번 구현 범위는 다음과 같다.

- 명확한 공격 패턴은 코드 규칙으로 판정하고, 후보가 없는 구간은 OpenAI를 호출하지 않는다.
- 소량의 애매한 `node` 요청은 제외하고 로그인 반복 실패, 오류 기반 경로 스캔,
  고속 대량 요청, 고속 다중경로 접근을 점수화한다.
- 경계 후보만 AI가 보조 검토하며 외부 입력인 원문 UA는 프롬프트에 넣지 않는다.
- 동일 IP·규칙 알림에 쿨다운을 적용하고 판정·텔레그램 발송 결과를 DB에 저장한다.
- 텔레그램 전송에 timeout과 제한적 retry를 적용하고 성공 여부를 호출자에게 반환한다.
- 인증 회원과 관리자 요청을 무조건 제외하지 않고 동일한 고위험 규칙으로 감시한다.
- IP별 상위 20개에 들지 않는 분산 로그인 공격도 전체 구간 집계 규칙으로 확인한다.
- 관리자 화면에 실제 30분 주기를 표시하고 최근 판정·발송 이력을 제공한다.
- 감지 규칙과 오탐 방지 조건을 자동 회귀 테스트로 고정한다.

### 구현 결과

- `security-log-detector.ts`를 추가해 로그인 실패 5회 이상, 오류 기반 다중경로
  스캔, 30분 120건 이상 고속 요청, 고속 다중경로 순회, 여러 IP에 분산된 로그인
  실패를 코드 규칙으로 판정한다.
- 로그인 실패 3~4회 또는 오류가 섞인 중간 규모 자동화 후보만 AI 보조 검토 대상으로
  남겼다. 후보가 없으면 OpenAI를 호출하지 않는다.
- AI 프롬프트에서는 원문 User-Agent를 제거하고 서버가 계산한 숫자 통계만 전달한다.
- `security_log_alerts` 테이블과 마이그레이션을 추가해 규칙, 위험도, 요약, AI 사용 여부,
  텔레그램 성공 여부, 중복 생략 여부를 기록한다.
- 동일 `규칙+IP`의 성공 알림은 6시간 동안 다시 보내지 않는다. 전송 실패 건은 쿨다운
  대상으로 보지 않아 다음 분석 때 다시 시도할 수 있다.
- 기존의 `admin` 전체 제외와 `로그인 계정+node UA` 전체 제외를 제거했다. 정상 사용은
  높은 규칙 임계값 아래에 머물러 알리지 않고, 계정 탈취 후 대량 자동화는 감시한다.
- 텔레그램 전송은 8초 timeout, 최대 2회 시도, 4,000자 제한, 성공 여부 반환을 적용했다.
- 프론트 API 프록시가 원본 IP와 UA를 HMAC 서명해 API로 전달하도록 했다. 공유 환경변수
  `SECURITY_PROXY_SECRET`이 양쪽에 설정됐을 때만 서명된 원본 정보를 신뢰한다.
- 관리자 API `GET /security-log/alerts`와 최근 판정·발송 이력 표를 추가했다. 즉시 분석
  결과에는 후보 수, 전송 수, 중복 생략 수, AI 보조 사용 여부가 표시된다.
- 관리자 안내 문구의 분석 주기를 실제 값인 30분으로 수정했다.
- `test-security-log-detector.mjs`로 단발 로그인/소량 node 요청 비탐지, 무차별 로그인,
  경로 스캔, 관리자 대량 요청, 분산 로그인 공격을 회귀 검증한다.

### 검증 결과

- API `npm run test:security-log` 통과.
- 프론트엔드 `npm run build` 통과.
- Railway와 Vercel production에 동일한 `SECURITY_PROXY_SECRET`을 민감 환경변수로 설정했다.
- API 배포 후 `security_log_alerts` 테이블 생성, `/security-log/alerts` 라우트 등록,
  30분 스케줄러 기동, 공개 API 200 응답을 확인했다.
- 프론트 production 배포 후 서명 프록시를 통과한 검증 요청이 DB에 원래 UA와 실제
  클라이언트 IP로 기록되고 더 이상 Vercel의 `node` UA로 기록되지 않는 것을 확인했다.

## 追記 (2026-09-03) — 동시 로그인 제한(409)이 brute_force로 오탐되던 문제

사용자가 실제로 온 텔레그램 알림 2건(`brute_force`, `rapid_multi_path`,
같은 IP `211.216.113.16`, 8분 새 30여 건)을 보고 "분석해달라"고 요청 —
`request_logs`를 직접 대조해 조사했다.

### 실측 결과: 진짜 브루트포스가 아니라 정상 사용자의 재로그인 시도였음
이 IP의 `/auth/login` 요청 30건이 **전부 401(비밀번호 오류)이 아니라
409**였다. 동시에 `/settings`·`/favorites`·`/courses`·강의 영상 재생·
질문/노트 등 다른 API는 전부 200/201 정상 — `hyunyg` 계정이 **이미
어딘가에 정상 로그인된 상태에서, 다른 곳에서 계속 재로그인을 시도하다가
"계정당 동시 로그인 1개 제한"(`auth.service.ts`의 `ConflictException`,
2026-07-31에 도입)에 막혀 8분간 409를 20회 이상 받은 것**이었다.

### 원인: `loginFailures` 집계가 409도 "실패"로 셈
`security-log-detector.ts`의 `buildIpStats()`가
`entry.status >= 400`이면 무조건 로그인 실패로 카운트했다. 409는
비밀번호가 틀린 게 아니라 "이미 로그인 중이라 거부됨"이라 전혀 다른
성격의 응답인데 구분하지 않았던 것 — `detectDistributedLoginAttack()`의
분산 공격 판정에도 같은 버그가 있었다.

### 조사 중 겪은 함정: DB 조회 시 타임존 리터럴 문제(2026-08-21 사례 재발)
`request_logs.ts`가 `timestamp without time zone` 컬럼이라, WHERE절에
`'...Z'`(UTC 접미사) 리터럴을 그대로 쓰면 postgres가 세션 타임존
기준으로 재해석해 엉뚱한 범위를 조회한다(2026-08-21 조사 때 이미 한 번
겪었던 문제인데 이번에 또 걸림). `'YYYY-MM-DD HH:MI:SS'`(타임존 접미사
없이)로 조회해야 정확하다 — 매번 잊지 않도록 재차 기록.

### 수정
- `buildIpStats()`의 `loginFailures`: `status >= 400 && status !== 409`로
  변경(409 제외).
- `detectDistributedLoginAttack()`의 `failures` 필터도 동일하게 수정.
- `scripts/test-security-log-detector.mjs`에 회귀 테스트 추가: 동일 IP에서
  `/auth/login` 409를 20회 반복해도 후보가 0건이어야 한다는 케이스.
  `npm run test:security-log` 통과 확인.
- Railway 배포 후 정상 기동(라우트 등록, 스케줄러 시작) 확인.

이걸로 정상 사용자의 재로그인 시도가 더 이상 브루트포스로 오인되지
않는다 — 진짜 자격증명 실패(401 등)로 인한 브루트포스 탐지는 그대로
유지된다.
